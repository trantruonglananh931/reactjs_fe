import React from 'react';
import RoleForm from '../../components/role/RoleForm';

const RoleFormPage = ({ isEdit }) => {
  return <RoleForm isEdit={isEdit} />;
};

export default RoleFormPage;